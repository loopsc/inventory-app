# inventory-app

App keeps track of books. The following properties can be tracked:

- If the book is read
- If the book is fiction

### Features

- Add and remove books from database
- Edit existing entries in the database

### Learning Outcome

- More advanced SQL queries
- Use of external library: HTML/CSS form styling library (SELECT2)
